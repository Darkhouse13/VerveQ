# VerveQ v2 QA Loop — Bug Log

Branch `claude/v2-qa-loop` off master `441a777`. Discovered against local build @441a777 + live Convex (`admired-warthog-495`), live verveq.com serves the same bundle generation.

Severity: P0 broken/crash · P1 wrong behavior/dead end · P2 ugly/cramped/misaligned · P3 nit.

| # | Sev | Where | What | Repro | Status |
|---|-----|-------|------|-------|--------|
| 001 | P0 | /v2/learn (signed out) | Error boundary with raw Convex error: `learn:getLearnReviewPlan` → "Not authenticated" (requireUserId convex/learn.ts:61). Learn entry route has no auth gate but its queries require identity. | Signed out → navigate /v2/learn | FIXED — /v2/learn* gated behind UsernameOnlyRoute (welcome?next=…); learnEntryHonestyContract |
| 002 | P2 | Error boundary (global) | Boundary body dumps raw server error text incl. request IDs and convex file paths to end users. Should show friendly copy; details to console only. | Trigger any render error | FIXED — ErrorBoundary shows humanizeServerError(); raw detail stays in console; errorHumanizingContract |
| 003 | P2 | /compete (desktop) | Top row of mode cards clipped at top edge — no top border/rounded corner visible; rows 2-3 fine. | Signed out → /compete, viewport 1280x657 | FIXED — compete grid centers with auto-margins + scrolls instead of clipping (justify-center overflow) |
| 004 | P1 | /v2/leaderboard | TENNIS filter chip offered — product is football-only. | Open leaderboard, look at sport chips | FIXED — sport filter is ["All","Football"]; footballOnlyScopeContract |
| 005 | P3 | /v2/leaderboard | Podium ELO shows raw float ("1241.9") while list rows show integers. | Open leaderboard podium | FIXED — leaderboard values render Math.round()ed |
| 006 | P0 | /learn/geography (+ /:nodeId) flag-on | v1 learn routes have no V2Redirect → reachable under flag-on, and crash signed-out (same Not authenticated as 001). Containment leak + crash. | Signed out → /learn/geography | FIXED — /learn/geography(+/:nodeId) wrapped in V2Redirect → /v2/learn; flag-off untouched |
| 007 | P1 | /learn/prototype | Prototype route reachable in prod; renders broken "Couldn't start this lesson" card with giant empty gap + orphan buttons. | Navigate /learn/prototype | FIXED — /learn/prototype wrapped in V2Redirect → /v2/learn |
| 008 | P2 | 404 (flag-on) | NotFound renders v1 gray max-w-md shell — off-brand inside v2 world; "Return to Home" → v1 home. | Navigate /nonexistent-xyz | FIXED — flag-on NotFound is neo-brutalist with Back to home → /v2; flag-off rendering byte-identical |
| 009 | P1 | /v2/higher-lower (desktop ≤~700px tall) | Screen is h-screen + overflow-hidden, content ~810px tall: HIGHER/LOWER buttons AND the Game Over panel render below the fold and are unreachable (page cannot scroll). Player cannot play or restart. | Viewport 1280x657 → open mode | FIXED — PlayStage center column scrollbar made visible (was scrollbar-none); column was already the scroll valve |
| 010 | P3 | Blitz results | TOPIC tile shows lowercase "football" raw value. | Finish a Blitz run | FIXED — Blitz results topic title-cased |
| 011 | P0 | Who Am I (backend) | Correct full name REJECTED. Curated `whoAmIApprovedClues.answerName` is abbreviated ("B. Matuidi") but `buildAnswerAliases` assumes full names — aliases are only ["B. Matuidi","B Matuidi","BM"]. "Blaise Matuidi" = wrong; bare "Matuidi" only passes as a 2-edit typo of "B Matuidi"; short surnames ("A. Ayew") leave NO winnable guess a human would type. | Play who-am-i, type the full name the clues describe | FIXED (deploy-gated) — canonical-name resolution + surname/initial aliases; whoAmIAnswerMatchingContract |
| 012 | P1 | Who Am I deduction board | Every guess shows "Nation ? Pos ? Club ?" — comparison metadata lookup never resolves guesses (same abbreviated-name keying); the deduction mechanic is dead. | Guess any famous player | FIXED (deploy-gated) — feedback resolves both guess and abbreviated answer via normalized metadata index |
| 013 | P2 | Who Am I reveal | Win/lose reveal prints abbreviated DB name: "It was B. Matuidi" — while clue 4 says "My first name is Blaise". | Finish a round | FIXED (deploy-gated) — reveal returns the canonical full name |
| 014 | P2 | Who Am I visual panel | Caption is internal design copy: "Blurred visual hook — identify the player from clues and deduction." over a placeholder gradient; photoUrl is always null. | Open who-am-i | FIXED — decorative "blurred visual hook" panel removed (never had a real image) |
| 009b | P1 | /v2/who-am-i same fold lock | Guess input + GUESS/REVEAL below the locked fold at ≤~700px viewport; after game ends, "Correct!/NEXT PLAYER/HOME" panel completely invisible — dead screen. | Viewport 1280x657, finish a round | FIXED — decorative panel removed shrinks Who Am I above the fold; end panel reachable via visible scroll |
| 015 | P0 | VerveGrid search (convex/verveGrid.ts searchPlayers) | Search is scoped to the active cell's validPlayerIds → (a) every famous-player query returns "No players found" — mode feels broken; (b) any result shown IS a correct answer → search is an answer oracle, wrong picks cost nothing, 9-guess budget meaningless. Copy promises "you only learn if it counts after you lock it in" — false. Needs full-roster search; correctness only on submitGuess. | Open any cell, type "messi" (nothing) then "foster" (only valid GKs) | FIXED (deploy-gated) — full-roster search via by_sport_name + new by_sport_lastName index; verveGridSearchContract |
| 016 | P1 | Duels create flow | Sport picker offers Basketball and Tennis (with working content) — violates football-only scope. "Sports trivia" card copy: "Football · Basketball · Tennis". | /v2/duels → NEW DUEL → Sports trivia | FIXED — duel sports flow locks football and skips the sport step; copy updated; footballOnlyScopeContract |
| 017 | P1 | Learn entry (LearnEntryScreen.tsx) | Fabricated dashboard: STREAK_DAYS=9 hardcoded, weekday chips always M–F green, "6 items due today" hardcoded count — shown to brand-new users. | Fresh user → /v2/learn | FIXED — streak panel replaced with real due-for-review panel from getLearnReviewPlan |
| 018 | P0 | Learn "Today's session" | Start button hardcodes `?node=geo.pipeline.proof` — internal pipeline-proof fixture ("PIPELINE-PROOF FIXTURE: TRIVIAL GEOGRAPHY FACTS…" banner, Paris-is-capital questions) served to real users instead of their due node. | /v2/learn → Start | FIXED — Start targets pickTodaysSessionNode(plan.nodes); disabled until plan loads; learnEntryHonestyContract |
| 019 | P1 | Learn runner MCQ options | Option key chip renders the full option text crammed in a 28px box ("France" squeezed, then "France" label again) — server sessions key options by text, UI assumes single letters. | Any live MCQ rung | FIXED — MCQ chips show index letters (A–D); option.key still submitted |
| 020 | P2 | Learn runner header | Node-title ribbon overlaps the left sidebar card and the bg text duplicates beneath it (banner rendered twice, one clipped under panel). | /v2/learn/run | FIXED — node ribbon truncates; type chip shrink-0 |
| 021 | P2 | Learn entry hero | "4 QUESTIONS · ≈4 MIN" + 4-type list is static fixture copy; actual session is 10 questions. | Compare hero vs run | FIXED — hero metaLine no longer claims a fake question count |
| 022 | P2 | Learn mastery/review | "last seen 2d ago" on nodes a brand-new user has never seen (and DUE IN 12H for 0-item queues) — placeholder-feeling scheduling copy. | Fresh user → mastery | FIXED — "last seen 2d ago" dropped; unscheduled nodes say "Not scheduled yet"; fixture fallbacks removed |
| 023 | P1 | Profile (desktop) | Left rail layout collision: "RANKED · SEASON 2"+LOCKED chip overlap EDIT/SHARE row; "PEEK AT THE LADDER" button overlaps the red sign-out warning text. | /v2/profile signed in, 1280px | FIXED — profile rail cards use min-h-fit + scrollable columns; no overlap at any height |
| 024 | P1 | Profile stats | GAMES PLAYED shows 1 after ~6 completed casual games (blitz, HL, who-am-i, arena, duel). Counter wired to the wrong source. | Play several casual modes, open profile | FIXED — tile reads profile.totalPlays (lifetime); HL/WhoAmI/VerveGrid now increment totalGames (deploy-gated). Duels/arena counting deliberately left as-is (guest participants, multi-path completion). |
| 025 | P2 | Profile achievements | "Multi-Sport Athlete — play 2+ different sports" achievement on a football-only product. | /v2/profile | FIXED — multi-sport achievement filtered out of the shell locker; footballOnlyScopeContract |
| 026 | P2 | Profile EDIT | Primary-styled EDIT button → toast "Profile editing is coming soon!" — dead end. | Click EDIT | FIXED — dead EDIT button removed; SHARE full-width |
| 027 | P1 | Arena join (bad code) | Raw Convex error shown to user: "[CONVEX M(challengeArenas:join)] … Uncaught Error: Arena not found at handler (../convex/challengeArenas.ts:1910:21)". Also stray "BACK TO CHALLENGE" label. | Arena → JOIN WITH CODE → ZZZZ99 | FIXED — join errors humanized ("Arena not found"); back goes to /v2/arena with "Back to Arena" label |
| 028 | P1 | Forge gate (username-only) | Locked screen presents "1,200 CURRENT ELO · 300 ELO to go" for unranked username-only users — fabricated ELO depth contradicting the parked-ranked model. | /v2/forge as username-only | FIXED — username-only Forge gate explains full-account requirement without fabricating ELO |
| 029 | P3 | Duel accept screen | "Basketball · Quiz · intermediate" — raw lowercase difficulty; "You can play as a guest" line shows even when viewer is signed in. | Open duel link signed-in | FIXED — difficulty title-cased; guest line only for signed-out viewers |
| 030 | P3 | Duels hub | Link-opponent duels show as "vs @Link opponent" — raw placeholder name. | Create link duel | FIXED — link duels show "Open invite" instead of "vs @Link opponent" |
| 031 | P1 | /results "TRY OTHER MODE" | Navigates to v1 `/sport-select?mode=survival` — leaves the v2 shell (and v1 sport-select offers non-football sports). | Finish a quiz → TRY OTHER MODE | FIXED — /sport-select V2Redirects → /compete; ResultScreen TRY OTHER MODE flag-aware → /compete |
| 032 | P1 | /v2/live-match + compete card | Compete card promises "Real-time 1v1, ELO matched" but the screen is only a viewer: "No live match yet → GO TO CHALLENGE" → /v2/duels. No matchmaking queue exists in v2 — dead-end card with a false promise. | Compete → Live Match | FIXED — Live Match tile removed (createFromChallenge has no callers — mode unstartable); route kept as legacy viewer; footballOnlyScopeContract |
| 033 | P1 | Upgrade stat continuity | After username-only → full upgrade, GAMES PLAYED reset 1 → 0 while copy promises "your casual progress carries over". Same wrong-source family as 024. | Upgrade then open profile | FIXED — same as 024 (lifetime plays survive the upgrade) |
| 034 | P1 | Quiz image questions (short viewport) | Image question card pushes ALL answer options below the locked fold (no scroll) while the timer runs — at ~657px the player can't see or click any option. Worst case of the 009 overflow family. | 1280x657 → /v2/quiz image Q | FIXED — quiz image clamped to max-h-[38vh] in the v2 view + visible column scrollbar |
