# ANCHOR_GRID — retired (FW-PR1c)

The quantile-anchor grid is retired by owner ruling FW-PR1c: slotting anchors
by rank quantile decoupled price from expected points wherever the proxy
distribution is non-uniform (measured: the MID 11.5 anchor slot carried a
7.41 proxy against a 13.33 position maximum).

Pricing is now **direct** — see `PRICE_REVIEW.md` (Phase B owner artifact),
`price-draft.json` (full 2,895-player draft price list) and the anchor-design
section of `PROXY_METHOD.md`. This file's git history holds the FW-PR1/FW-PR1b
grids if they are ever needed for reference.
